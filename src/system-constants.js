const userValueDefault = 0;

const statuses = {
    NEW: "new",
    PENDING: "pending",
    DENIED: "denied",
    APPROVED: "approved",
    DONE: "done"
}

module.exports = {
    userValueDefault,
    statuses
}